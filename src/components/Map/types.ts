export type MapProps = {
    style?: object,
    showsUserLocation?: boolean,
    scrollEnabled?: boolean,
    zoomEnabled?: boolean,
    rotateEnabled?: boolean,
    showMarker?: boolean,
    timeInterval?: number
}